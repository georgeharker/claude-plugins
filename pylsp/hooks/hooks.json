{
  "description": "Opt-in (off by default): on Python edits, run a fallback mypy check (PYLSP_MYPY_ON_EDIT=1) for harnesses that don't honor the plugin-supplied .lsp.json.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/run-mypy.sh",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
