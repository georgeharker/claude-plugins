{
  "description": "Opt-in (off by default): on Python edits, autofix+format (RUFF_FIX_ON_EDIT=1) and/or report diagnostics (RUFF_DIAGNOSTICS_ON_EDIT=1).",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/run-ruff.sh",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
