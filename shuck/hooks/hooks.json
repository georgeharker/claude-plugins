{
  "description": "Opt-in (off by default): on shell edits, autofix (SHUCK_FIX_ON_EDIT=1) and/or report diagnostics (SHUCK_DIAGNOSTICS_ON_EDIT=1).",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/run-shuck.sh",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
